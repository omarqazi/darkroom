server {
    listen 80;
    server_name darkroom.smick.tv;
    root /home/app/darkroom/public;

    passenger_enabled on;
    passenger_user app;

    # If this is a Ruby app, specify a Ruby version:
    passenger_ruby /usr/bin/ruby2.4;

}